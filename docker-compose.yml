services:
  php-benchmark:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      PARSER_WORKERS: ${PARSER_WORKERS:-1}
    mem_limit: 1536m
    memswap_limit: 1536m
    cpus: 2.0
